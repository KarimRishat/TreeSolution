#pragma once
#include <iostream>
#include "Tree.h"
template<typename T>
std::ostream& operator << (std::ostream& o, const TreeSpace::Tree<T>* tree) {
	operator<<(o,tree->root.)
	return o;
}

template<typename T>

